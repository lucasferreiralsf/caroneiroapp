kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:
- name: backend
  image: node:12
  commands:
  - cd caroneiro-api
  - npm install
  - npm run build
  - npm run start:prod

trigger:
  branch:
  - NestJS-V2